PACKAGE_NAME="logitech-trueforce"
PACKAGE_VERSION="1.0"
# Build in-place against the `source` symlink that dkms always creates
# (-> /usr/src/$pkg-$ver). The `build` subdir DKMS exposes is NOT
# guaranteed to be populated with the source files, which led to silent
# no-op builds where `dkms install` reported success but kept the old
# .ko in /lib/modules (issue #17).
MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/source modules"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/source clean"
BUILT_MODULE_NAME[0]="hid-logitech-dd"
BUILT_MODULE_LOCATION[0]="../source"
DEST_MODULE_NAME[0]="hid-logitech-dd"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"